<Project Sdk="Microsoft.NET.Sdk.Web">
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Nerdbank.Streams" Version="2.11.74" />
  </ItemGroup>

  <ItemGroup>
    <!-- Legacy client: deliberately references ONLY the core package (no gRPC).
         This proves that an unchanged WebSocket client keeps working against a
         server that has additionally enabled the gRPC transport. -->
    <ProjectReference Include="..\..\..\src\ReverseTunnel.Yarp\ReverseTunnel.Yarp.csproj" />
  </ItemGroup>
</Project>
